[project]
name = "ccl25-eval"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai>=2.38.0",
]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
