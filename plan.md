# CCL25 古诗词理解与推理任务总体技术路线

## 0. 核心判断

本任务包含三类能力：

| 子任务 | 输入 | 输出 |
| --- | --- | --- |
| 词语释义 | 古诗词全文 + 指定词语 | 词语在诗中的含义 |
| 句子理解 | 古诗词全文 + 指定诗句 | 对应白话文译文 |
| 情感推理 | 古诗词全文 + 四个情感选项 | 最符合情感的选项字母 |

数据规模很小，官方训练数据只有百余首诗词，不能把这些数据当作主要知识来源。合理目标不是让模型通过小数据重新学习古诗词知识，而是让模型学会：

1. 比赛任务格式；
2. 答案颗粒度；
3. 结构化 JSON 输出；
4. 情感选项比较方式；
5. 古诗词解释与翻译的输出风格。

主办方已确认可以使用开源模型作为教师。因此总体路线应收敛为：

```text
A：强开源基座 + Prompt baseline
BC：Answer-only QLoRA + 开源教师蒸馏混合训练，作为主力路线
BCD：在 BC checkpoint 上加入循环中间 block，作为结构增强实验
```

默认采用优先级：

```text
BC 默认作为最终候选
BCD 只有稳定超过 BC 时晋级为最终候选
A 只作为 baseline，不作为优先提交方案
```

也就是说，BCD 不是天然优于 BC。它只有在本地验证和消融中稳定超过 BC，才作为最终提交方案；否则 BC 是最稳主线。

---

## 1. 总体架构

整体流程分为四层：

```text
原始数据
  -> 数据标准化
  -> 开源教师生成结构化解释信号
  -> 质量过滤与人工抽查
  -> BC 混合训练学生模型
  -> BCD 循环 block 结构增强实验
  -> 分任务评测与最终路由
```

这里的关键原则是：

> 教师模型负责提供高质量解释信号，学生模型最终仍然学习稳定、简洁、可提交的答案格式。

不要把长 CoT 原样作为主要训练目标。长 CoT 容易让学生模型在最终输出中生成解释、废话或非 JSON 内容。更稳的方式是把教师 CoT 压缩成短证据、选项分析和最终答案。

---

## 2. 模型分工

### 2.1 教师模型

教师模型必须是开源模型。规则允许教师模型超过 20B，可以使用更强的开源模型生成解释、伪标注和选项分析；
教师模型用途：

1. 为词语释义生成上下文解释依据；
2. 为句子翻译生成句意骨架；
3. 为情感题生成意象线索、情感标签和选项排除理由；
4. 发现官方训练样本中可拆分、可增强的训练信号；
5. 生成少量可人工抽查的伪标注样本。

教师模型不直接用于最终提交推理。

### 2.2 学生模型

学生模型必须满足最终提交限制：

```text
开源
20B 及以下
可通过 vLLM 或 Transformers 稳定推理
```

候选优先级：

```text
14B 级中文/推理能力强的开源指令模型
> 8B/7B 级轻量模型
```

学生模型训练目标：

1. 学会按比赛格式输出；
2. 学会利用短证据进行情感判断；
3. 学会控制答案长度；
4. 学会严格生成合法 JSON；
5. 保持基座模型已有的古诗词知识，不被小数据过拟合破坏。

---

## 3. 数据设计

### 3.1 标准输入格式

所有样本统一成同一种输入结构：

```json
{
  "idx": 0,
  "title": "李端公",
  "author": "卢纶",
  "content": "故关衰草遍，离别自堪悲。...",
  "qa_words": ["衰草", "故关", "风尘"],
  "qa_sents": ["故关衰草遍，离别自堪悲", "掩泪空相向，风尘何处期。"],
  "choose": {
    "A": "欢快的重逢",
    "B": "仕途的无奈",
    "C": "对未来的期待",
    "D": "惜别的感伤"
  }
}
```

统一输出格式：

```json
{
  "idx": 0,
  "ans_qa_words": {
    "衰草": "枯黄的草，烘托荒凉离别的氛围"
  },
  "ans_qa_sents": {
    "故关衰草遍，离别自堪悲": "旧关一带长满枯草，分别本就令人悲伤"
  },
  "choose_id": "D"
}
```

### 3.2 训练样本类型

BC 混合训练使用三类样本。

#### 类型 1：Answer-only 样本

```text
输入题目 -> 最终 JSON
```

这是最重要的稳定性样本，用于保证模型最终输出简洁、合法、符合比赛格式。

#### 类型 2：Short-evidence 样本

```text
输入题目 -> 短证据 -> 最终 JSON
```

短证据可以包括：

1. 关键词的上下文解释；
2. 诗句的句意骨架；
3. 情感意象线索；
4. 选项排除理由。

短证据要短，不要写成长篇赏析。

#### 类型 3：Teacher-critique 样本

```text
输入题目 + 候选错误答案 -> 错误原因 -> 修正后的最终 JSON
```

这种样本主要用于情感选择题，帮助模型学会比较相近选项。

---

## 4. 开源教师蒸馏

### 4.1 教师输出格式

教师模型输出必须结构化，避免自由长文。

推荐格式：

```json
{
  "word_clues": {
    "衰草": "枯草，暗示荒凉与离别"
  },
  "sentence_skeleton": {
    "故关衰草遍，离别自堪悲": "旧关荒凉，分别令人悲伤"
  },
  "emotion_clues": ["故关", "衰草", "离别", "掩泪"],
  "option_analysis": {
    "A": "重逢不符",
    "B": "仕途无奈不是主旨",
    "C": "未来期待不明显",
    "D": "符合惜别感伤"
  },
  "final_answer": {
    "choose_id": "D"
  }
}
```

### 4.2 教师数据过滤

教师生成数据进入训练前必须过滤：

1. `choose_id` 必须是 A/B/C/D；
2. 输出不能缺少指定词语或指定句子；
3. 词语释义不能明显脱离诗句上下文；
4. 句子翻译不能变成赏析；
5. 情感分析不能选项和理由矛盾；
6. JSON 必须可解析；
7. 随机抽样人工检查。

### 4.3 人工抽查策略

建议按样本类型抽查：

| 样本类型 | 抽查重点 |
| --- | --- |
| 词语释义 | 是否解释了词语在诗中的意思，而不是现代常用义 |
| 句子翻译 | 是否忠实、通顺、不过度扩写 |
| 情感选择 | 是否选项匹配，理由是否自洽 |
| Teacher-critique | 是否真的指出了错误，而不是编造理由 |

人工抽查比例不必很高，但每轮数据生成后都要做。小数据场景下，少量错误伪标签就可能明显伤害模型。

---

## 5. BC 混合训练主线

BC 是最终主力路线，由 B 的答案稳定性和 C 的教师解释信号融合而成。

### 5.1 阶段一：Answer-only Warmup

先只用 answer-only 样本训练：

```text
输入题目 -> 最终 JSON
```

目标：

1. 固定输出 schema；
2. 控制答案长度；
3. 避免模型输出解释；
4. 建立稳定基础 checkpoint。

### 5.2 阶段二：BC Mixed Distillation

在阶段一 checkpoint 上继续训练，混合：

```text
answer-only
short-evidence
teacher-critique
```

初始比例建议：

```text
answer-only：60%
short-evidence：30%
teacher-critique：10%
```

如果格式错误率升高，提高 answer-only 比例；如果情感题准确率不足，提高 short-evidence 和 teacher-critique 比例。

### 5.3 阶段三：Answer-only Replay

混合训练后，再用少量 answer-only 样本 replay：

```text
输入题目 -> 最终 JSON
```

目标是把模型从“解释训练模式”拉回“比赛输出模式”。

这一步很重要。它可以降低模型最终输出 CoT、说明文字、Markdown 或非 JSON 的概率。

---

## 6. BCD 循环 Block 实验

循环 block 不作为一开始的主线，而是在 BC checkpoint 稳定后进行。

### 6.1 实验目标

循环中间 block 的目标是：

```text
在不改变最终模型规模太多的情况下，提高有效推理深度。
```

它可能对情感推理有帮助，但也可能伤害翻译和格式稳定性。因此必须通过消融验证。

### 6.2 推荐循环位置

只循环中间 1/3 Transformer block：

```text
前 1/3 层：不循环
中间 1/3 层：重复一次
后 1/3 层：不循环
```

不要循环最前层或最后层：

1. 前层更接近 token/位置表示，重复后容易造成分布偏移；
2. 后层更接近输出对齐，重复后容易破坏 JSON 稳定性；
3. 中间层更适合语义组合和推理增强。

### 6.3 三档实验

#### BCD0：直接循环，不继续训练

```text
BC checkpoint + 推理时中间 block 重复一次
```

用途是快速判断循环是否严重破坏模型。若 BCD0 大幅下降，不代表 BCD1 一定失败，但说明后续必须继续训练适配。

#### BCD1：循环结构 + 继续 QLoRA

```text
BC checkpoint
-> 修改 forward，中间 block 重复一次
-> 使用 BC 混合数据继续训练
```

这是主要结构增强实验。

#### BCD2：门控循环结构

如果 BCD1 不稳定，可以加入门控：

```text
h1 = middle_blocks(h)
h2 = middle_blocks(h1)
h = h1 + alpha * (h2 - h1)
```

`alpha` 初始化为 0 或 0.1。这样模型可以自己学习是否使用第二次循环。如果循环无益，模型可以退化回接近 BC 的行为。

### 6.4 训练和推理一致

除 BCD0 外，真正提交候选必须满足：

```text
训练时循环
推理时也循环
```

不要用普通结构训练、循环结构提交。这样 hidden state 分布不一致，风险很高。

---

## 7. 实验矩阵

建议保留清晰消融，而不是一次性堆所有技术。

| 编号 | 模型 | 数据/训练 | 结构 | 目的 |
| --- | --- | --- | --- | --- |
| A0 | base | zero-shot prompt | 原始结构 | 最低基线 |
| A1 | base | few-shot prompt | 原始结构 | prompt 上限 |
| B0 | base | answer-only QLoRA | 原始结构 | 格式和任务适配 |
| BC0 | B0 | short-evidence 混合蒸馏 | 原始结构 | 检验教师短证据收益 |
| BC1 | B0 | 长 CoT 训练 | 原始结构 | 风险对照，不优先提交 |
| BC2 | BC0 | answer-only replay | 原始结构 | 主力提交候选 |
| BCD0 | BC2 | 无继续训练 | 循环中间 block | 快速探测结构影响 |
| BCD1 | BC2 | 继续 QLoRA | 循环中间 block | 主结构增强候选 |
| BCD2 | BC2 | 继续 QLoRA | 门控循环 block | 稳定版结构增强 |

预期最可能胜出：

```text
BC2 或 BCD1
```

BC1 主要作为对照，用来验证“长 CoT 直接训练是否会破坏输出格式”。除非它明显优于 BC2 且格式稳定，否则不建议作为最终路线。

---

## 8. 评测与消融

### 8.1 分任务指标

每个实验都要分任务记录：

| 实验 | 词义分 | 翻译分 | 情感分 | 格式错误率 | 总分 |
| --- | ---: | ---: | ---: | ---: | ---: |
| A0 | | | | | |
| A1 | | | | | |
| B0 | | | | | |
| BC0 | | | | | |
| BC2 | | | | | |
| BCD1 | | | | | |

情感题看 accuracy；词义和翻译可以用自动指标加人工抽查。即使官方只给总分，本地也必须拆分分析，否则无法判断教师蒸馏和循环 block 到底提升了哪一类任务。

### 8.2 重点观察

BC 路线重点看：

1. 教师短证据是否提升情感题；
2. 教师短证据是否改善词义解释准确性；
3. 混合训练后 JSON 错误率是否升高；
4. answer-only replay 是否能恢复输出稳定性。

BCD 路线重点看：

1. 循环 block 是否提升情感推理；
2. 是否损害句子翻译；
3. 是否导致输出变长；
4. 是否增加格式错误；
5. 是否只在个别 fold 上有效。

### 8.3 任务路由

如果比赛允许按题型使用不同 checkpoint，可以做简单路由：

```text
词语释义：BC2
句子翻译：BC2
情感推理：BC2 或 BCD1，取验证集更好的那个
```

如果必须单模型提交，则选择总分最高且格式错误率最低的模型。

---

## 9. 风险控制

### 9.1 小数据过拟合

风险：

```text
模型记住训练样本风格，但泛化到评测集变差。
```

控制：

1. 使用 QLoRA 而不是全参数微调；
2. 控制 epoch；
3. 做 early stopping；
4. 保留 answer-only replay；
5. 使用交叉验证或稳定 dev split。

### 9.2 教师伪标签污染

风险：

```text
教师错误会被学生放大。
```

控制：

1. 教师输出结构化；
2. 自动过滤明显错误；
3. 人工抽查；
4. 多教师一致性投票可作为高质量标记；
5. 伪标签样本权重不要过高。

### 9.3 长 CoT 破坏格式

风险：

```text
模型最终输出推理过程，而不是 JSON。
```

控制：

1. 不把长 CoT 作为主训练目标；
2. 使用 short-evidence；
3. 最后做 answer-only replay；
4. 推理时使用 JSON schema / guided decoding；
5. 加输出后处理和重试机制。

### 9.4 循环 block 破坏表示分布

风险：

```text
循环后 hidden state 分布偏移，翻译下降或格式错误增加。
```

控制：

1. 只循环中间 1/3 层；
2. 只循环一次；
3. 循环后继续训练；
4. 必要时使用门控循环；
5. 分任务比较，不盲目全局使用。

---

## 10. 推荐执行顺序

### 第一步：建立 A baseline

```text
运行 zero-shot / few-shot prompt
记录三类任务表现和格式错误率
确定学生基座模型
```

### 第二步：训练 B0

```text
构造 answer-only 数据
QLoRA 训练
验证 JSON 稳定性和基础效果
```

### 第三步：生成教师数据

```text
开源教师生成结构化 short-evidence
自动过滤
人工抽查
形成 BC 混合训练集
```

### 第四步：训练 BC

```text
B0 checkpoint
-> mixed distillation
-> answer-only replay
-> 得到 BC2
```

### 第五步：测试 BCD

```text
BC2 checkpoint
-> BCD0 快速探测
-> BCD1 继续训练
-> 必要时尝试 BCD2
```

### 第六步：最终选择

```text
如果 BCD1 总分稳定超过 BC2，提交 BCD1
如果 BCD1 只提升情感题，做任务路由
如果 BCD1 不稳定，提交 BC2
```

---

## 11. 最终推荐路线

最推荐的主路线是：

```text
14B 级开源学生模型
+ answer-only QLoRA warmup
+ 开源教师 short-evidence 蒸馏
+ answer-only replay
+ JSON schema / guided decoding
+ 输出后处理
```

在这个稳定主线之上，再测试：

```text
中间 1/3 Transformer block 循环一次
+ 继续 QLoRA 适配
+ 分任务消融
```

最终判断标准不是“技术是否复杂”，而是：

1. 总分是否提升；
2. 情感题是否稳定提升；
3. 翻译题是否没有明显下降；
4. JSON 格式错误率是否足够低；
5. 多个验证划分上是否稳定。

一句话总结：

> 先用 BC 融合路线做出稳定强学生，再把循环 block 当作 BC 之上的结构增强实验；若循环带来稳定收益就采用，否则回退到 BC。
